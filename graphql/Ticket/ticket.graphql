type Ticket {
    id: ID!
    title: String!
    description: String!
    priority: String!
    state: String!
    attached_file: String!
    user: User
}

extend type Query {
    tickets: [Ticket!]! @all
    findTicket(id: ID @eq): Ticket @find
}
